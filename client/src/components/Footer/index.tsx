import React from "react";
import { Social } from "../Social";
import { StyledFooter } from "./StyledFooter";

export const Footer = () => {
  return (
    <StyledFooter>
      <Social/>
    </StyledFooter>
  );
};
